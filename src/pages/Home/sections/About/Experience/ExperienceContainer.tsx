import React, { PropsWithChildren } from 'react';

function ExperienceContainer({ children }: PropsWithChildren) {
  return <aside className="">{children}</aside>;
}

export default ExperienceContainer;
