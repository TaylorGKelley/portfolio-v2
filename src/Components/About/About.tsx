import React from 'react';
import './About.css';
import SkillCarousel from '../ui/SkillCarousel/SkillCarousel';

export default function About() {
  return (
    <section id="About">
      <SkillCarousel />
    </section>
  );
}
